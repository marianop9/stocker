import { productService } from "$lib/service/products.service";
import type { PageLoad } from "./$types";

export const load: PageLoad = async ({fetch}) => {
    // const products = productService.list(fetch)

    // return {
    //     products,
    // }
}